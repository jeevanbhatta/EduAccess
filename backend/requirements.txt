Flask==2.3.2
Flask-Cors==5.0.0
python-dotenv==1.0.0
azure-cognitiveservices-speech==1.41.1
moviepy==1.0.3
pytesseract==0.3.10
Pillow==9.2.0
Werkzeug==2.3.7
pdfminer.six==20201018
python-docx==0.8.11
# requests is used for calling Azure Vision REST endpoints; don't install the deprecated
# `azure` meta-package — install only service-specific SDKs when needed. The project
# already includes the speech SDK below.
requests==2.31.0
# pytesseract & Pillow for text extraction from images or Braille logic